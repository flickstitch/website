= simple_form_for(@scene) do |f|
  = f.error_notification

  .form-inputs
    = f.association :project, :selected => Project.last.id
    = f.input :name
    = f.input :desc
    = f.input :start_date
    = f.input :end_date

  .form-actions
    = f.button :submit
